Sources for the plugin `plugin.applovinMax.Smaato`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Smaato" = {
            publisherId = "com.solar2d",
        },
    },
}
```
